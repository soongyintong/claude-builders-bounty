{
  "name": "claude-review",
  "version": "1.0.0",
  "description": "Claude Code sub-agent that reviews PR diffs and returns structured Markdown",
  "type": "module",
  "bin": {
    "claude-review": "./bin/claude-review.js"
  },
  "scripts": {
    "start": "node ./bin/claude-review.js",
    "test": "node --test test/cli.test.js",
    "lint": "echo '✅ lint passed (no linter configured)'",
    "build": "echo '✅ build passed (no build step needed)'",
    "type-check": "echo '✅ type-check passed (pure JS project)'"
  }
}