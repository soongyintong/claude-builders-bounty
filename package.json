{
  "name": "claude-builders-bounty-hooks",
  "version": "1.0.0",
  "private": true,
  "description": "Claude Code hooks for the Claude Builders Bounty repository.",
  "type": "module",
  "scripts": {
    "type-check": "node --check hooks/pre-tool-use/block-dangerous-bash.js && node --check test/block-dangerous-bash.test.js",
    "test": "node --test",
    "lint": "node scripts/check-format.js",
    "build": "node --check hooks/pre-tool-use/block-dangerous-bash.js && node --check scripts/install-block-dangerous-bash.js"
  },
  "devDependencies": {}
}
