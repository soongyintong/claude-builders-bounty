{
  "name": "claude-builders-bounty",
  "version": "0.1.0",
  "description": "Claude Code PR review agent for structured Markdown reviews.",
  "type": "module",
  "bin": {
    "claude-review": "./bin/claude-review.js"
  },
  "scripts": {
    "type-check": "node --check bin/claude-review.js && node --check lib/review.js && node --check test/review.test.js && node --check scripts/lint.js",
    "test": "node --test",
    "lint": "node scripts/lint.js",
    "build": "node bin/claude-review.js --help"
  },
  "keywords": [
    "claude-code",
    "pull-request",
    "review",
    "agent"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
